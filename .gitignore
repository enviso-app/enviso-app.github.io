# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local secrets (the app now asks each user for their own key)
.env
.env.local

# Written by tools/fetch-announcement.mjs on each build, like other build
# output. Committing it would mean a card could only change via a developer.
public/announcement.json
